package cpu_test

// NewChip8
// should return a Chip8 in stopped state

// Chip8.Run(program)
// if the CPU is stopped, should start the CPU
// should execute the specified program
// should stop the CPU once the program terminates

// Chip8.Load(program)
// if the Chip8 is running, should have no effect
// if the Chip8 is stopped, should prepare a Chip8 program such that
